
import s from "./ButtonAdd.module.scss";

const ButtonAdd = () => {
    return (
        <button type="button" className={s.button}>
            add+
        </button>
    );
};

export default ButtonAdd;
